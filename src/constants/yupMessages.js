const yupMessages = {
  nameRequire: "Name must be required",
  emailRequire: "Email must be required",
  emailSyntax: "Email must be in correct syntax",
  passwordRequire: "Password must be required",
};

export default yupMessages;
